'use client'

import { WorkspaceUser } from '@/types'

type Props = {
  workspaceUser: WorkspaceUser
}

export default function DashboardSidebarDropdown({ workspaceUser }: Props) {
  return <div>DashboardSidebarDropdown</div>
}
